Once Ruby and jekyll are installed [correctly](https://jekyllrb.com/docs/installation/):

* use `bundle exec jekyll server` for local preview
* use `bundle exec jekyll build` for local build (debugging)
